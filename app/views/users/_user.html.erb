<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if can?(:destroy, user) && !current_user.correct_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete,
    data: { confirm: t(".sure") } %>
  <% end %>
</li>
